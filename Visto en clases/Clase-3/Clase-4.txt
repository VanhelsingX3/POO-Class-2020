Ambiente de desarrollo es un profucciòn donde ya algo se subio a la nube y se puede probar. No es lo mismo que un ambiente local.

Configuraciòn de proceso de transpilaciòn

npm run tsc -- --init
npm install

"scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc"
  },